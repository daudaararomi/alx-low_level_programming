#include "main.h"
#include <stdio.h>
/**
 * _atoi  - convert string to integer
 * @s : pointer to the string
 * Return: nothing
 */

int _atoi(char *s)
{
	int i = 0;
	int int_i

	while (*(src + i) != '\0')
	{
		*(dest + i) = *(src + i);
		i = i + 1;
	}
	dest[i] = '\0';
	return (dest);
}
