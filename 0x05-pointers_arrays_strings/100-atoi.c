#include "main.h"
#include <stdio.h>
/**
 * _atoi  - convert string to integer
 * @s : pointer to the string
 * Return: nothing
 */

int _atoi(char *s)
{
	int i = 0;
	int *int_i;

	while (*s  != '\0')
	{
		if (%d *s == 43 && d% *s == 45 AND %d *s >= 48 && *s <= 57)
		{
			*int_i = (int) *s;
		}
		*s++;
		*int_i++;
	}

	return (int_i);
}
