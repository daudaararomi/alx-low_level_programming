#include "main.h"

/**
 * swap - swap the values of two integers
 * @a : first integer
 * @b : second integer
 * @c : temporary integer
 * Return: nothing
 */

void swap_int(int *a, int *b)
{
	int c;
	c = a;
	a = b;
	b = c
}
